{
  "name": "example",
  "version": "1.0.0",
  "description": "Example-project for real-spawn",
  "scripts": {
    "example:node": "real-spawn node example",
    "example:echo": "real-spawn echo example",
    "example:cmd-dir": "real-spawn dir",
    "example:bash-ls": "real-spawn ls",
    "example:tsc-watch": "real-spawn tsc --watch --listEmittedFiles",
    "example:tsCleaner-watch": "real-spawn ts-cleaner --src source --dist compiled --watch --verbose",
    "example:run-parallel": "run-p example:tsc-watch example:tsCleaner-watch"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "real-spawn": "file:..",
    "ts-cleaner": "^1.0.3",
    "typescript": "^3.7.2"
  }
}
